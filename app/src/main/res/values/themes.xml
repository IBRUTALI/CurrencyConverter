<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CurrencyConverter" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/elem_blue</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondaryVariant">@color/elem_blue</item>
        <!-- Status bar. -->
        <item name="android:statusBarColor">@color/bg_blue</item>
    </style>

    <style name="Theme.CurrencyConverter" parent="Base.Theme.CurrencyConverter" >
        <item name="android:windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
    </style>
</resources>