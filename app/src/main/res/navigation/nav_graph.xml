<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/selectCurrencyFragment">

    <fragment
        android:id="@+id/selectCurrencyFragment"
        android:name="com.ighorosipov.currencyconverter.features.converter.presentation.screens.select_currency.SelectCurrencyFragment"
        android:label="@string/select_currency"
        tools:layout="@layout/fragment_select_currency" >
        <action
            android:id="@+id/action_selectCurrencyFragment_to_convertResultFragment"
            app:destination="@id/convertResultFragment" />
    </fragment>
    <fragment
        android:id="@+id/convertResultFragment"
        android:name="com.ighorosipov.currencyconverter.features.converter.presentation.screens.convert_result.ConvertResultFragment"
        android:label="@string/result"
        tools:layout="@layout/fragment_convert_result" />
</navigation>